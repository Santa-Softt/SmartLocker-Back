type Mutation{
    initiateHold(size: LockerSize!): ActiveRental!
}

type ActiveRental {
    id: ID!
    locker: Locker!
    startTime: String!
    estimatedEndTime: String!
    isPenalized: Boolean!
}